# 独立服装窗口与工作区导出方案

最初方案日期 2026-09-14。2026-09-15 用户已明确授权同时完善 Mesh 插件与服装系统，以下历史“待确认后开发”限制被此次授权取代。当前实施范围以 [四分类与服装工程需求](OWOTS_FOUR_CATEGORY_WORKSPACE_REQUIREMENTS.md) 为准；保留历史方案用于解释设计演进。

## 游戏内窗口

默认以 `/ ?` 物理键切换独立窗口，保持按住不重复开关；允许重新绑定。Esc 先清空当前子操作或关闭弹出框，再关闭服装窗口。文本输入时不触发快捷键；窗口关闭后释放鼠标/键盘捕获，避免影响原生菜单。使用 REF 的绘制能力，但不要求展开 REF 主菜单。

顶部为标题、关闭按钮、搜索框和“列表 / 卡片”切换。左侧为服装、武器分类；中央显示条目；右侧详情显示大图、名称、描述、作者（若提供）、当前状态和“应用外观”按钮。底部固定显示加载状态及错误信息。“恢复原版”按类别操作，不与删除 MOD 混用。

列表适合大量条目，每行缩略图、名称、简短描述、已应用标记；卡片使用同一批条目与稳定 ID，窗口宽度决定列数。切换显示模式保留搜索、分类、焦点及选中项。浏览选中只更新详情，点击应用才换装；应用完成前不显示已应用。缺图使用占位框，缺描述显示简短空状态，不拒绝条目。

使用深色半透明面板、浅色正文和单一强调色，当前应用与键盘焦点采用不同标记。焦点必须清晰可见；Tab 顺序跟随视觉顺序，Enter 应用当前详情项。搜索/模式按钮不可只有无说明图形。静态尺寸验收覆盖 1280×720、1920×1080 和较高 DPI；实机另验鼠标、快捷键冲突及游戏输入是否泄漏。

保持现有线程边界：绘制读取不可变快照，点击仅投递命令，游戏更新回调执行原生操作。图片加载与释放需遵循图形资源线程约束；不能用随意加载游戏纹理指针代替 UI 图片所有权。

## 与原生菜单同步

### 当前实现进度

源码候选已改用 ImGuiRender 独立窗口，加入 Slash/Escape 开关、搜索、分类、列表/卡片浏览、名称/描述/作者详情和显式应用按钮，runtime 引用编译通过。图标当前只有数据字段，尚无图片渲染；不能视为卡片视觉完成。旧实机周期 23 尚未替换。

当前 REF 的 REFramework.cpp 在 m_draw_ui 为真时才按 WantCaptureMouse/Keyboard 拦截游戏消息。独立窗口关闭 REF 主界面后可能输入穿透；需要额外输入接入并验证，不通过打开 REF 主菜单来规避独立窗口要求。生成的全站设计检索结果偏向网页叙事，不适合此界面，未采用；只采用针对键盘导航、可见焦点的交互建议。

REF 源码现有候选补丁：主菜单隐藏时依据 ImGui 捕获请求管理 m_external_ui_active，覆盖窗口消息、DirectInput、光标解锁及结束后的恢复；尊重 Input Passthrough。衣橱按帧请求鼠标和键盘捕获，关闭后不再续请求。未安装，需验证失焦、关闭、插件卸载、按键抬起及鼠标/手柄路径，不能仅凭编译成功确认无输入穿透。

图标候选已接入：原生私有 ABI 解码 PNG/JPEG，图片上限 4 MiB、2048×2048，缓存缩放到最长边 256、最多 128 个句柄。ImGui 动态图集负责 GPU 上传；每次绘制回取 UV 和 TexRef。刷新或卸载请求释放，渲染线程统一回收；受控上下文销毁前撤销矩形，下次绘制重新分配。缺失接口/图片与错误图片显示占位内容，不影响条目。

原生 Release 和 C# runtime 引用编译均通过。独立 CTest 已验证合成 RGBA 像素、缺失/损坏文件、CPU 图集增长/重建和延迟释放；不包含 GPU 或视觉验收。新的原生 DLL 和 bundled-wardrobe-icons 尚未安装。

设置候选新增 preferences.json，独立于每槽外观记录。界面支持 Slash 或 F6–F12 绑定、列表/卡片偏好，以及保存记录、自动恢复、原生明确选择同步开关；实验功能默认关闭。启动后由游戏更新线程应用设置，修改单纯显示偏好不会重置未改变的保存子系统。离线往返、错误配置保护及 runtime 编译通过，重启与实际按键行为仍未实测。

共同状态区分“原生实际服装/武器”和“MOD 外观”。独立窗口应展示两者；选择原生服装使用已验证的原生装备流程，选择纯外观 MOD 不改变实际武器。

原生服装菜单打开时，根据共同状态重建展示。优先插入有独立映射的 MOD 行，选中状态、名称、预览和确认回调共同接入；若暂时只能加当前外观状态栏，必须标记为部分实现，不能当作双向同步已经完成。禁止用运行时 Prefab ID 直接冒充 ItemID，也禁止只改可见行下标而忽略虚拟列表索引。

从原生菜单明确确认另一套服装时，应更新共同状态并清除对应 MOD 覆盖；仅打开或浏览菜单不应清除。同步应有来源标记，防止原生回调和独立 UI 相互反复触发。菜单关闭、取消、场景过渡及读档均需要独立验证。

现有候选为 GUI030106 的 CostumeList、setup/onUpdateItem/callback_Select/callback_Decide/applyToPreview/applyCostume。先确认数据与副作用，再接入；原生菜单只能在存档点打开不意味着独立窗口也必须受这一入口限制。

## Mesh 工作区导出：待确认方案

现有实现位于 `RE-Mesh-Editor-main/modules/workspace`：export_tracking 维护来源和导入基线，export_pipeline 规划选中集合及依赖，export_manifest 发布与回滚，export_operators 和属性/UI 模块提供入口。新增输出目标“独立服装系统包”，保留现有替换式目录/PAK 输出。

建议先完成一套服装或一件武器的包，继续以 Outliner 明确选择集合；不选集合不能变成导出全部。导入时记录来源游戏、源逻辑路径、部位角色、关联 MDF、可追溯模板依赖；保留原始基线，不通过重新导入覆盖用户编辑。旧工程提供显式关联来源与部位，不根据模糊名称静默猜测。

包设置包含稳定 MOD ID、名称、类型、描述、可选图标；每个部位指定模型集合、Prefab/Catalog 模板及依赖策略。首次导出需要写出包内必需的未修改资产，不能直接复用现有“只输出变化文件”的结果；后续同一包可按所有者增量更新。

输出目录建议：

```text
<输出>/<MOD ID>/
  reframework/data/owots_appearance_lab/mods/<MOD ID>/manifest.json
  reframework/data/owots_appearance_lab/mods/<MOD ID>/icon.png  (可选)
  natives/<平台>/mods/<作者>/<条目>/...独立资产及数字版本后缀
```

具体图标字段、平台前缀和路径编码能力需与运行时实现锁定后确定。稳定 ID 不依赖 Blender 集合名称；资源文件路径由包映射统一生成，不能以仅改文件名的方式生成包。

流水线为：检查来源和所选部位 → 构建完整依赖图 → 分配独立路径 → 导出 Mesh/MDF/TEX → 使用格式解析器改写 Prefab/Catalog/MMI/相关配置引用 → 输出描述与图标 → 检查引用闭合、文件存在与冲突 → 暂存区成功后回滚安全发布。禁止通过固定字节长度替换作为通用格式写入器。

默认将需要私有化的模型、材质、作者纹理、骨骼/物理及局部配置放入包；游戏公共资源可明确保留引用。未知依赖、模板缺失或不能可靠改写时列出缺失项，不能产出“成功但不完整”的包。这里只检查路径和依赖，不新增骨名、层级、权重或动画兼容性验证；原有 Mesh 序列化要求与新增服装注册条件需区分。

导出保持不直接部署游戏目录，复用已有暂存、外部修改冲突检测及回滚发布机制。导出的游戏派生资产由用户本地持有，不纳入插件源码发布。

## 原生菜单适配接口补充（2026-09-15，源码与元数据依据）

独立菜单已获用户基本操作确认，槽位 4 自动恢复本次复测通过。原生菜单适配仍需以下联动，不能以单向取消逻辑冒充完成：

| 行为 | 已定位接口 | 实现约束 |
| --- | --- | --- |
| 增加可浏览条目 | `GUI030106.cCostumeList._DisplayItemList`、`_DisplayItemCount`、`_FsgCostumeItemGrid` | 使用菜单会话内的稳定 MOD ID 映射；不能将列表索引当作原生装备 ID，也不能只增加网格数量 |
| 名称与详情 | `_TxtCostumeName`、`_TxtDescription`、`_TxtCategoryName` | 原生 `callback_Select` 和 `onUpdateItem` 会刷新显示，需在对应刷新路径处理；浏览中的原生服装不能被错误标注为已应用 MOD |
| 选中标记和图标 | `_PnlEquipIconPath`、`_TexThumbnailPath`、`_ThumbnailTextureCtrls` | 原生图标加载机制与独立 ImGui 图集不同，不能直接传入 ImGui 图标句柄 |
| 预览 | `isPreviewDataReady`、`applyToPreview`、`_ModelPreviewModule` | 菜单预览对象与实际角色不同，需分别管理资源生命周期；预览不得提前改变实际装备或写入存档 |
| 确认与退出 | `callback_Decide`、`applyCostume`、`onClose` | MOD 确认进入现有外观切换队列；原生确认使用原生装备逻辑。关闭本身不算明确选择；临时模型 ID 不进入原生存档 |

新元数据证据保存在工作区私有 `native-menu-display-metadata.json`。此表为实际接口约束和后续实现范围，目前没有注入原生 MOD 条目或改写原生文本。原生明确改选取消 MOD 的运行验收正在等待用户操作，保持当前已安装代码以收集证据。

## 衣橱视觉与交互 V2（2026-09-15）

用户反馈独立菜单工作正常，但布局不够美观、功能不明确；要求单击选中显示预览，双击应用。视觉提案见 `OWOTS_WARDROBE_UI_CONCEPT_V2.png`。图片为生成的设计示意，服装插图及额外条目不是实机资产或已安装 MOD。

- 顶部：标题、当前实际应用的服装/武器、设置、关闭。实际应用与浏览焦点分别显示。
- 左侧约 58%：服装/武器分类、搜索、卡片/列表切换及可滚动条目。卡片金色边框表示预览焦点，绿色“已应用”标记表示实际状态；两者允许落在不同条目上。
- 右侧约 42%：固定预览图、名称、作者、描述、主操作“应用此外观”和次操作“恢复原版服装/武器”。单击仅切换详情；双击图片或名称触发同一个应用队列。应用中和已应用条目禁止重复提交。
- 底部：短状态及“单击预览 · 双击应用 · Esc 关闭”。保存设置、刷新与恢复诊断收进设置，避免和日常换装争夺注意力。
- 使用当前 Dear ImGui/Hexa.NET.ImGui 的表格、子窗口、Selectable、DrawList 和局部样式栈实现，保留中文字体。深炭灰背景、暖金强调、4px 左右圆角；不影响 REF 其他窗口的全局样式。
- 预览本阶段是作者图标/预览图，缺失时显示占位；实时 3D 预览还需独立模型生命周期实现，不能将示意图宣称为 3D 功能。

双击逻辑已写入候选源码；整套视觉布局目前仍是设计提案，不是已部署界面。原生菜单因剧情未解锁而暂缓实机验证，实验同步已关闭。

用户随后确认 V2 设计基本符合需求，并明确只需要图片预览，不开发实时 3D 预览。无图 MOD 使用统一的管理器占位，默认图片待定；当前为固定尺寸深色区域与居中文字。已实现 V2 布局、局部炭灰/暖金样式、已应用名称、卡片选中边框、收起设置、窄窗口纵向布局及单击/双击队列逻辑。运行引用编译通过，候选包 SHA256 `52cead5fced1ad8ad7ced02e87e84e70f59e0655e9bf16ba55c2a583348ba755`；部署与人工结果按交接/AGENTS 最新记录判断。

图片比例补充：原 DLL 已保持图片比例，但图片控件占用大小随图片比例变化。新原生候选改为固定方框、等比居中与留边，使横图、竖图及无图占位布局一致。真实 ImGui 绘制路径的合成横/竖图测试及 Release 构建通过；该 DLL 尚未安装，不影响正在验收的 C# V2。

## Mesh/PFB 服装工程流程补充（2026-09-15，设计中）

用户提出在导入 Mesh 时同时导入 PFB，让作者编辑。可设计为关联导入，但 PFB 是对象、组件和资源引用的模板，不只是路径重定向表；完整服装还可能需要 Catalog、MMI、MDF、TEX、骨骼/物理等依赖。不能以单独复制 PFB 代替依赖闭合。

建议保留现有“导入 Mesh/原路径替换导出”，增加“导入服装工程”入口。输入既可为 PFB（向下解析引用，加载各 Mesh），也可为 Mesh（根据资源引用索引寻找 PFB，多个候选让作者选择）。同一个 Mesh 可被多个 PFB 引用，没有唯一候选时不能按文件名猜测；找不到 PFB 时允许继续普通建模，标记工程缺少模板，稍后补齐。

导入流程：选择游戏与来源 → 选择 PFB/服装模板或 Mesh 关联候选 → 展示部位及依赖 → 导入可视模型与材质 → 保存源文件基线和配置覆盖层。每个部位保留稳定 ID、源逻辑路径、实际文件版本/哈希、模板关系；集合重命名不改变身份。普通/HQ 关联应显式呈现，不能假定文件名关系。

作者工作界面分两层：

- 常用“服装资源”面板：名称、描述、图片、服装/武器类型、部位集合、Mesh/MDF、MMI、骨骼/物理资源及独立/共享策略。用户选择资源或 Blender 对象，由插件维护引用，避免手填全部长路径。
- 高级“Prefab 结构”面板：只读起步的 GameObject/组件树，随后开放已验证字段的类型化编辑。可以用 Empty 表达空间层级，但不把所有组件硬塞成可视场景对象；原生结构与 Blender 场景表现分开。未知字段、实例引用及组件顺序保留在基线中，不伪造默认值。首版不提供任意组件删除/新增/跨类型编辑，待格式往返验证后扩展。

保存工程时保留三份职责：原始资源快照/哈希（只读基线）、作者对受支持字段的修改、发布时生成的独立路径映射。`.blend` 保存稳定关联和必要配置，完整原始模板可放工作区伴随文件；项目重新打开须检查伴随文件是否存在。导入不直接把所有路径改为 MOD 路径，也不把当前编辑态重设为原始基线。

导出对话框增加目标“替换式 MOD / 独立服装包”。独立包：选择条目和部位 → 分配 `mods/<作者>/<条目>/…` 资源路径 → 对 Mesh/MDF/TEX 及选定骨骼/物理导出 → 通过结构化写入器改写 PFB/MMI/Catalog 等资源引用 → 生成运行时 manifest → 检查依赖闭合与资源冲突 → 暂存区成功后发布。需要私有化的未修改资源首包也要输出；明确保留共享的游戏公共资源不复制、不改写为虚假的包内文件。后续增量同时比较编辑内容和路径映射/依赖配置变化，不能只看 Mesh 是否改动。沿用现有选中集合范围、所有者冲突检测及回滚发布，不直接部署游戏目录。

技术起点：现有 `export_tracking.capture_baseline` 已保存导入基线，`export_manifest.publish` 已实现所有者/哈希及暂存发布；可在其上增加工程配置与依赖计划。工作区 `REE-Content-Editor/ContentPatcher/FileLoaders/PfbFileLoader.cs` 展示基于 ReeLib 的 `PfbFile.Read`、`SetupGameObjects`、`RebuildInfoTables` 和保存机制，可评估独立进程桥接而非要求 Blender Python 直接运行 .NET。是否引入该依赖、打包方式与许可需实施前核对；当前未增加依赖。

实施顺序建议：先真实鬼武者模板的无修改读写往返与引用核对 → 只读关联导入 → 支持资源字段编辑与独立包导出 → 高级组件编辑。读写失败、未知引用无法保存时明确阻止独立包发布，保留普通 Mesh 工作能力。检查限于格式/依赖/资源完整性，不添加骨骼兼容性验证或动画重定向。

以上为方案，尚未修改 Mesh 插件代码。首版建议“关联 PFB + 常用资源字段编辑”，避免一开始变成完整通用 Prefab 编辑器。

## 待确认的工程选项

- 一套包是否允许包含多套可选服装，还是首版一个包对应一个条目？建议首版一包一条目，保留未来集合格式。
- 导入入口偏好：先选原生服装模板再替换模型，还是从已有替换式 MOD 一键建立独立工程？建议两者共用依赖模型，先做用户现有 MOD 转换流程。
- 图标使用导入图片还是 Blender 渲染当前模型？建议先支持图片并预留渲染按钮。
- 多部位映射采用显式表格还是集合标记？建议集合存储稳定角色，导出对话框展示并允许调整。

以上为方案选项，不要求用户现在答复。确认后才实现 Mesh 插件改造。
