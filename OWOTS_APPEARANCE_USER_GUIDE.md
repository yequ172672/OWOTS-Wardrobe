# 鬼武者外观系统：原型使用说明

本说明对应 2026-09-14 的开发原型。原生服装菜单尚未增加 MOD 条目。服装、武器切换及早期 REF 内嵌菜单已有实机确认；保存记录写入已验证，但用户报告槽位 4 自动恢复失败，该问题尚未验证修复。

当前 C# 候选版已在编译周期 25 加载通过，设置文件写入及原生菜单钩子安装通过运行检查。外观已清回原版，持久化与原生菜单同步关闭。2026-09-14 22:14（北京时间）游戏正常退出后，配套自定义 REF DLL 已备份并安装，未重新启动。下文的独立衣橱操作说明针对待验收候选版，不能当作已经完成实机验收的功能。具体接手步骤见 [人工验收交接](OWOTS_APPEARANCE_HANDOFF.md)。

## 在游戏中切换

2026-09-15 用户反馈独立 MOD 菜单工作正常，基本操作已获人工确认。图标、焦点切换、手柄等细项仍按各自验证记录判断，不能由这一反馈推定全部通过。

1. 进入可操作状态，保持游戏运行。暂停可能使原生换装无法推进。
2. 在配套版本安装后，按键盘 `/ ?` 所在键打开独立“鬼武者 · 外观衣橱”，按同一键或 Esc 关闭。设置中可改为 F6–F12；文本输入期间不切换窗口。快捷键、输入拦截和焦点行为仍待实机验收。
3. 选择“服装”或“武器”，通过列表或卡片浏览，支持名称及描述搜索。点击条目仅查看图标、名称、作者和描述；点击“应用外观”才开始切换，完成后显示“已应用”。
4. 取消时点击“恢复原版服装”或“恢复原版武器”。两类选择独立，不会因取消武器而清除服装。

新增描述文件后点击“刷新已安装外观”。显示名称可以相同，但 MOD ID 必须不同。相同 ID 的所有冲突定义都会被拒绝，界面显示原因；其他有效 MOD 仍可使用。

选择新的外观时，系统先恢复该类别原版并等待旧资源停止使用，再加载新资源。因此切换期间可能短暂显示原版。新资源加载失败时，该类别保持原版；另一类别不受影响。清理超时会保留资源，恢复游戏运行后再尝试取消，不要在此时热重载插件。

## 实际装备与外观

请继续通过游戏原生菜单选择实际武器。外观系统只选择显示用资源，不把 MOD 模型来源当成玩家实际装备，也不保存一份独立的原生武器属性。

当前不支持 MOD 属性注册。武器伤害与防御判定数值的专项测试按用户要求暂缓；已验证的范围包括实际装备 ID 保持及外观切换。未来属性支持必须遵循需求文档中的规则：有明确注册的 MOD 属性时优先采用，否则使用当前实际装备的原生属性。

## 安装 MOD 描述与资产

每套 MOD 的描述文件放在游戏目录下：

```text
reframework/data/owots_appearance_lab/mods/<MOD文件夹>/manifest.json
```

最小示例：

```json
{
  "schemaVersion": 1,
  "id": "author.outfit_a",
  "name": "服装 A",
  "kind": "outfit",
  "parts": [
    {
      "part": "BODY",
      "catalog": "mods/author/outfit_a/body_catalog.user",
      "prefab": "mods/author/outfit_a/body.pfb"
    }
  ]
}
```

这些路径是资源的逻辑路径，省略磁盘文件的数字版本后缀。需要另外提供实际 Catalog、Prefab 和依赖资产，按游戏松散文件加载目录结构安装。仅写一个 JSON 不会生成可加载的模型。完整字段与部位列表见 [核心说明](appearance-core/README.md)。图标、特殊逻辑和属性不是必填项。

可选图片写法：在描述中添加 `"icon": "preview.png"`，将图片放在该 `manifest.json` 同目录，也可使用 `images/preview.jpg` 这样的子目录相对路径。支持 PNG/JPEG，文件不超过 4 MiB、宽高不超过 2048 像素；加载后缩至最长边 256 像素。卡片和右侧预览共用这张图片。没有图片或图片加载失败时显示统一文字占位，仍可正常应用 MOD；管理器默认图片待定，不需要实时 3D 预览。添加图片后通过衣橱“设置”中的刷新操作重新加载。

资产应使用自己的路径，Prefab、材质及其他配置中的引用也必须指向相应独立资源。当前测试发现模型材质配置 MMI 可以覆盖 MDF 的纹理绑定，因此只修改 MDF 可能仍显示原版纹理。转换替换式 MOD 时需要沿实际依赖处理 MMI 等引用，不能只移动网格文件。

系统不验证骨名、层级、绑定、动画兼容性，不修复骨骼，也不做动画重定向。MOD 作者负责资产符合游戏本身要求。已有私有样本验证了独立物理资源路径；不同作者自制骨架的完整适用范围尚未实测。

## 存档记录现状

外观选择记录单独放在 `reframework/data/owots_appearance_lab/saves`，不修改游戏原生存档格式。记录包含用户索引、请求槽号、原生流程 UniqueID，以及两个 MOD ID。需要迁移时，将该目录与对应游戏存档一同备份；不要把记录文件改名后当作另一份存档的记录。

实际测试已确认手动槽位 4 和自动槽位 101 在游戏保存成功后分别写入正确记录。自动保存的准备阶段不会直接提交，真正写入阶段使用准备时捕获的外观选择。

槽位 4 首次自动恢复确实超时，后续手动调试重试完成且模型路径检查通过，但没有人工画面确认。失败后的自动槽位 101 曾记录部分外观，测试现已关闭持久化。候选版增加了未解决恢复期间禁止新快照、连续读档隔离、暂停期间不消耗恢复超时等保护；这些保护不能证明原问题已经修复。

2026-09-15 复测更新：从原版状态读取槽位 4，用户确认服装与两倍武器自动恢复成功；日志无恢复错误，NORMAL 状态独立服装路径检查通过。本次自动恢复验收通过。首次超时的根因及连续读档、不同暂停时序的覆盖仍未完成。本次运行已启用持久化与自动恢复，磁盘默认开关仍关闭，可通过衣橱设置保存开关。

候选衣橱的“设置”可启用外观记录及实验性自动恢复，默认均关闭。快捷键、列表/卡片模式和开关写入独立的 `preferences.json`；它与按存档区分的外观记录不同。直接调试命令只改变当次运行状态，不自动持久化设置。设置保存、重启恢复及连续读档仍需完整实机验收。

## 当前边界

- 已有实机反馈：独立 manba 服装及纹理、普通状态与所测 CG 保持、两倍武器对照、REF 菜单选择、分类取消与共存。
- 候选源码已实现：独立窗口、列表/卡片、搜索和详情、可选 PNG/JPEG 图标与缺图占位、快捷键及设置存储；编译和相关静态测试通过，画面、输入和重启验收未完成。
- 原生菜单同步候选目前只有“原生明确确认某类别后取消该类别 MOD”，默认关闭且未实机验证。MOD 条目、状态和预览进入原生菜单尚未完成，不能称为双向同步。
- 尚待完成：自动读档恢复修复验证、跨场景资源生命周期、独立 UI 实机验收、最终安装打包。武器属性与自定义逻辑暂缓。
- Mesh 插件导入和工作区导出集成仅有方案，等待用户确认后再开发，见 [衣橱与导出方案](OWOTS_WARDROBE_UI_AND_EXPORT_PLAN.md)。

本仓库提供源码与说明，不包含私有测试用游戏派生资产。研究细节和验证证据见 [运行时研究](OWOTS_APPEARANCE_RUNTIME_RESEARCH.md) 与 [菜单及存档研究](OWOTS_APPEARANCE_UI_SAVE_RESEARCH.md)。
